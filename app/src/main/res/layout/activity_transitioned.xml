<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
                xmlns:tools="http://schemas.android.com/tools"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:paddingLeft="@dimen/activity_horizontal_margin"
                android:paddingRight="@dimen/activity_horizontal_margin"
                android:paddingTop="@dimen/activity_vertical_margin"
                android:paddingBottom="@dimen/activity_vertical_margin"
                android:orientation="vertical"
                tools:context="com.gelormini.materialdesignexample.activities.TransitionsActivity">

    <TextView
        android:text="@string/hello_world"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="24sp"
        android:textColor="@color/accent"
        android:layout_gravity="center_horizontal"
        />

    <!-- tint is required for the color animation. If it's not there, the image will disappear when the tint is set to 0 in animation. -->
    <ImageView
               android:id="@+id/hero"
               android:layout_width="300dp"
               android:layout_height="300dp"
               android:tint="#00000000"
               android:transitionName="home_icon"
               android:layout_marginTop="10dp"
               android:layout_gravity="center_horizontal"/>

</LinearLayout>
