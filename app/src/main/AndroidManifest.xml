<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="co.touchlab.materialdemo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">
        <activity
            android:name=".HomeActivity"
            android:label="@string/app_name"
            android:launchMode="singleTop"
            android:theme="@style/NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
            android:name=".RecyclerGridActivity"
            android:label="@string/title_activity_recycler_grid">
        </activity>
        <activity
            android:name=".RecyclerListActivity"
            android:label="@string/title_activity_recycler_list">
        </activity>
        <activity
            android:name=".PaletteActivity"
            android:label="@string/title_activity_pallete"
            android:parentActivityName=".HomeActivity">
        </activity>
        <activity
            android:name=".RecyclerStaggeredActivity"
            android:label="@string/title_activity_recycler_staggered">
        </activity>
        <activity
            android:name=".AnimationActivity"
            android:label="@string/title_activity_animation"
            android:launchMode="singleTop"
            android:parentActivityName=".HomeActivity">
        </activity>
        <activity
            android:name=".TransitionedActivity"
            android:label="@string/title_activity_transitioned"
            android:launchMode="singleTop"
            android:parentActivityName=".TransitionedActivity">
        </activity>
    </application>

</manifest>
